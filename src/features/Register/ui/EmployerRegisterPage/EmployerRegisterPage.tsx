import styles from './EmployerRegisterPage.module.scss';

const EmployerRegisterPage = () => {
    return (
        <div className={styles['worker-register-page']}>
            EmployerRegisterPage
        </div>
    );
};

export { EmployerRegisterPage };
